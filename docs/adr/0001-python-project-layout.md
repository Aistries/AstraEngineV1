pyproject.toml is the primary project configuration.
uv is the package manager.
Dependencies are split into base, development, and production.
requirements.txt is retained only as a compatibility entry point.